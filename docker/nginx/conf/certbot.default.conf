server {
  listen 80;
  listen [::]:80;

  server_name magento-preview.castledigital.com.br www.magento-preview.castledigital.com.br;
  server_tokens off;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }
}
